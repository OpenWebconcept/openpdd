const Save = (props) => {

    // This data will be fetched from the OpenPUB API. The function for this can be found in /assets/js/frontend/components/fetch-openpub-data.js.
    return (
      <ul className="openpdd-news"></ul>
    );
};

export default Save;
